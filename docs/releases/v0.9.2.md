只修文档与版本号的一致性问题，无功能改动。

## 问题

v0.9.1 发布时，README 顶部的「当前版本」与「控制台页签数」没有跟着更新（还写着 v0.9.0 / 11 个页签）。
市场精选列表的评审规则里明确写着会「对着描述与仓库逐条核对数字」，包里 README 与实际版本不一致属于自找麻烦。

## 修法

- README 的「当前版本」对齐到本版；
- 「控制台页签」从 11 改为 **12**（v0.9.0 新增了「智能体」页签，漏改了）。

## 升级

无破坏性改动。已经装了 v0.9.0/v0.9.1 的不必为此单独升级。
