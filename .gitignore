# ── 依赖 ────────────────────────────────────────────────────────────────────
node_modules/
package-lock.json

# ── 演练实体数据：绝不入库 ──────────────────────────────────────────────────
# SQLite 事实库、证据、攻击文件、扫描结果都落在 $DSH_HOME/redteam/，
# 若因调试被复制到仓库目录，这些规则保证它们不会被误提交。
engagements/
runs/
toolkit/
# 知识库是全局的（$DSH_HOME/redteam/knowledge.db + pocs/），同样不入库
pocs/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.jsonl
*.har
*.pcap
*.pcapng
*.log

# ── 凭据与密钥 ──────────────────────────────────────────────────────────────
.env
.env.*
*.pem
*.key
*.crt
*.p12
secrets/
secret*
token*
credential*

# ── 编辑器 / 系统 ───────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~
